class Role {
  id: string;
  affiliation: string;
  status: string;

  constructor(id: string, affiliation: string, status: string) {
    this.id = id;
    this.affiliation = affiliation;
    this.status = status;
  }
}
